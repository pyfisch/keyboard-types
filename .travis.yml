language: rust
rust:
  - stable
  - beta
  - nightly
  - 1.18.0
sudo: false
matrix:
  allow_failures:
    - rust: nightly
script:
  - cargo build --verbose
  - cargo build --features "heap_size serde" --verbose
  - cargo test --verbose
